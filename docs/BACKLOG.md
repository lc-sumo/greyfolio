# Backlog — asks from review, mapped to the build order

| Ask | Where it lands | Status |
|---|---|---|
| Reps cannot add their own deals; only admin | Phase 4 — every deal write is `requireRole('admin')`; no rep write route exists | Done |
| Adding a draw takes optional term + factor rate and computes payback and the merchant's payment | Phase 4 follow-up — `newDraw` terms, `paymentFor`, draw ledger sublines, add-draw form | Done |
| Say "override" everywhere, never "rip" | Codebase already uses Override / `overrideRate` throughout (teams included); only the handoff prototype file uses "rip" and it is the untouched reference | Done |
| Set the team leader from the Settings tab alongside reps | Phase 7 — Settings › Teams: name, leader (any active rep), override %; picking a leader moves them onto the team and grants Team lead access. Settings › Reps: team, rates, access, active, View as | Done |
| Renewals available to every rep for THEIR renewals, to know when to follow up | Phase 6 — `My renewals` in the rep portal (`/api/me/renewals`, server-scoped to deals they open/close/override, with merchant contact for follow-up and their own est. share; other reps appear only as "Closer"/"Opener", never by name) plus the admin Renewals board | Done |
| Statements should just be pay history: every payout, when, how much, which deal | Phase 6 — `Pay history` replaces Statements (`/api/me/payments`): every ledger row grouped by payout date with the run, deal, line and amount; recoveries as red lines; day and lifetime totals | Done |
| Renewal mark stays at 40% (workbook), and a **Prospecting** trigger fires 30 days post funding (workbook `ADDITIONAL CAPITAL AFTER`): the merchant is eligible for more capital | Phase 6 — `renewalOf` computes `prospectingDate`; Deal Status is derived like the sheet (Performing → Prospecting at 30d → Refi Ready at the mark) unless set by hand to Refinanced / Default / Slow Pay / Paid In Full; renewals get a Prospecting tab and a "More capital" column; both thresholds editable in Phase 7 Settings | Done |
| Consolidation commissions paid in increments: at entry ask for upfront share, number of increments, cadence and whether the rest is spread or paid when increments are done, then project when to expect receipts (owners' bookkeeping) | Phase 7 follow-up — schedule model gained `upfrontPct`, `cadenceDays`, `remainder: spread \| at-end`; new-deal form has a Payout structure block (lender default / all upfront / increments); the deal drawer shows every expected receipt with dates, overdue flags, next expected, and recorders for upfront, increments and the final; lenders carry structure defaults in Settings; Funding overview shows "Expected from lenders · 30 days" and overdue; the board pill turns red with "· late" | Done |
| A 20-increment consolidation shows 4/20 received — pay the rep for those 4 and keep count of increments paid | Payroll follow-up — on incremental segments the payroll unit is now one lender receipt per role (`F12|Opener|base|u3`), so the ledger itself records "4 of 20 increments paid". Payroll rows show collected vs uncollected units and select the collected ones by default (+ uncollected opt-in); the deal drawer shows "Paid to reps 4/20" per role; the rep's deal shows "4 of 20 increments paid to you"; pay history lists each increment. Legacy whole-segment rows still count as fully paid | Done |
| Zebra rows on every table; merchant contact stacked (name / email / phone) with click-to-call | Portal — `.table .tr:nth-child(even)` and `.srow` tints; shared `Contact` component (mailto + `tel:`) on Master deals, Payroll, Paid-in-run, Renewals, Merchants and the deal drawer | Done |
| Recording a lender increment adds to the rep's ledger | Domain — `repLedger` gained `accrued` (rep share on commission the lender has paid) and `awaitingLender`; **owed = max(0, accrued − paid − held)**. Wallet "Balance owed" and "Awaiting lender", roster, payroll rep list and the rep's deal rows (new `Awaiting lender` payout status) all read it. Answers README open question #3: reps are owed on collection | Done |
| Increment payment structures are consolidation-only, never LOC or LOC draws | Domain — `ProductRule.incremental` (true for both CONSOLIDATION products in the workbook seed, toggle in Settings › Product rules); `priceDeal` and `addDraw` only build a schedule for incremental products, whatever the lender's terms; the New deal form shows the Payout structure block only for those products and says so for the rest | Done |
| Drawers open twice as wide | Portal — `.drawer` is 1240px (was 620); New deal is a 3-column form with the live math pinned beside it | Done |
| Entering an existing client's email on New deal pulls up their profile and links the deal | Portal — the email is matched against the board as you type: an "Existing client" card lists every prior deal (CRM ID, funded date, lender · product, amount, lender paid, status) and fills business, contact and phone from the account (cleared again if the email stops matching); the deal saves under the same merchant email, which is how merchants group | Done |
| New deal: wider page and a bigger live-math panel; payment computed from amount, rate and term; referral % locked to Settings with the partner's cap and excess shown; PSF entered as % or $ | Portal — drawer is 1500px with a 3-column form beside a taller live-math panel (merchant, commission, referral and split sections). Payment = payback ÷ payments in the term at the chosen frequency (`paymentFor`). Referral % is read-only and **the domain ignores any typed rate** (`priceDeal` always uses the partner's settings rate); the cap is monthly per the workbook, so `createDeal` nets fees already owed to that partner on same-month deals, and the form shows fee before cap, owed so far, excess not paid. PSF has a %/$ toggle; dollars convert to the stored % | Done |
| CRM integration that fires triggers (e.g. renewal-ready → CRM task) | After Phase 9 — needs the real CRM (open question #6: URL pattern, API) before anything beyond the deep link | Later |

Payment math for a draw: `payback = amount × factor`; payments in the term at the deal's frequency are Daily = term, Weekly = term/5, Bi-Weekly = term/10, Monthly = term/21 (business days); `payment = payback / payments`.
